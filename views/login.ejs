<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="min-h-screen w-full bg-zinc-900 text-white flex flex-col items-center justify-center">
    <h1 class="text-3xl  mb-6">Welcome Back!</h1>

    <div class="w-full max-w-md bg-zinc-800 p-6 rounded-lg shadow-lg">
        <form action="/login" method="post" class="flex flex-col">

            <input type="email" name="email" required placeholder="Enter Your Email"
                class="p-3 mb-4 bg-zinc-700 border border-white rounded-md" />

            <input type="password" name="password" required placeholder="Enter Your Password"
                class="p-3 mb-4 bg-zinc-700 border border-white rounded-md" />
                <% if(error){ %>
                    <p class="text-red-600 mb-3"><%= error %></p>
                <% } %>

            <button type="submit" class="p-3 bg-green-600 hover:bg-green-700 rounded-md transition duration-200">
                Log In
            </button>
            
            <p class="mt-4 text-sm text-center text-gray-300">
                Don't have an account?
                <a href="/register" class="text-blue-400 hover:underline">Create one</a>
            </p>
        </form>

    </div>
</body>

</html>